---
import BaseLayout from './BaseLayout.astro';

type Props = {
  title: string;
  description: string;
  pathname: string;
};

const props = Astro.props;
---

<BaseLayout {...props} hasParticles>
  <slot />
</BaseLayout>
