---
import CtaBand from '../components/astro/CtaBand.astro';
import PageHeader from '../components/astro/PageHeader.astro';
import { listFreeResources } from '../lib/content';
import { routes } from '../lib/routes';
import MarketingLayout from '../layouts/MarketingLayout.astro';

const resources = listFreeResources();
---

<MarketingLayout
  title="Free Resources"
  description="Free gated resource library for Silkroad private-server operators."
  pathname={Astro.url.pathname}
>
  <section class="page-shell">
    <div class="container">
      <PageHeader
        title="Free Resources"
        copy="Publicly discoverable, but account-gated before download. This keeps the library indexable while protecting delivery."
        breadcrumbs={[
          { label: 'Home', href: routes.home },
          { label: 'Free Resources' },
        ]}
      />

      <div class="resources-grid">
        {resources.map((resource) => (
          <a class="resource-card link-card" href={resource.href}>
            <div class="meta">{resource.meta}</div>
            <h3>{resource.title}</h3>
            <p>{resource.description}</p>
          </a>
        ))}
      </div>
    </div>
  </section>

  <CtaBand
    title="Login to claim free resources"
    copy="Free resources stay visible for discovery, but claiming files, copy packs, and checklists happens through your account dashboard."
    primaryLabel="Login to Claim"
    primaryHref={routes.dashboard}
  />
</MarketingLayout>
